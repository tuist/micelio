base_url = "https://micelio.dev"
title = "Micelio"
description = "A headless Git forge whose source of truth is a write-ahead log in object storage."
default_language = "en"
compile_sass = true
build_search_index = false
generate_feeds = false

[markdown.highlighting]
theme = "github-light"
